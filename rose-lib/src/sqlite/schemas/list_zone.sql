CREATE TABLE x(
    zone_type_name TEXT,
    local_name TEXT,
    zon_file_path TEXT,
    start_point_name TEXT,
    respawn_point_name TEXT,
    is_underground INTEGER,
    bgm_day_path TEXT,
    bgm_night_path TEXT,
    sky_id INTEGER,
    minimap_path TEXT,
    start_x INTEGER,
    start_y INTEGER,
    decorations_path TEXT,
    buildings_path TEXT,
    day_cycle INTEGER,
    morning_time INTEGER,
    day_time INTEGER,
    evening_time INTEGER,
    night_time INTEGER,
    pvp_code INTEGER,
    planet_number INTEGER,
    zone_type INTEGER,
    camera_id INTEGER,
    join_trigger TEXT,
    kill_trigger TEXT,
    death_trigger TEXT,
    server_area_size INTEGER,
    name_id TEXT,
    weather INTEGER,
    experience_a INTEGER,
    experience_b INTEGER,
    boarding_pat INTEGER,
    respawn_point_number INTEGER,
    respawn_point_x INTEGER,
    respawn_point_y INTEGER,
    sea_type INTEGER,
    water_texture_id INTEGER,
    world_map_number INTEGER
);